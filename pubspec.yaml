name: redux_scopedmodel_bloc
description: Companion Source Code

version: 1.0.0+1

environment:
    sdk: ">=2.2.2 <3.0.0"

dependencies:
    flutter:
        sdk: flutter

    flutter_redux:
    scoped_model:
    rxdart:
    flutter_sparkline:
    cupertino_icons: ^0.1.2
    fish_redux: ^0.3.1
    provider: ^3.2.0
    flutter_hooks: ^0.7.0

dev_dependencies:
    flutter_test:
        sdk: flutter

flutter:
    uses-material-design: true
    # To add assets to your application, add an assets section, like this:
    # assets:
    #  - images/a_dot_burr.jpeg
    #  - images/a_dot_ham.jpeg
